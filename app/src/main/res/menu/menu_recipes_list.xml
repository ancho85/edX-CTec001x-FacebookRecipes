<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".recipemain.ui.RecipeMainActivity">
    <item
        android:id="@+id/action_main"
        android:icon="@android:drawable/ic_menu_gallery"
        android:title="@string/recipemain.menu.action.main"
        app:showAsAction="always"/> <!-- esto hace que siempre se vea como boton en pantalla -->

    <item
        android:id="@+id/action_show_all"
        android:title="@string/global.menu.action.all"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_show_favs"
        android:title="@string/global.menu.action.favs"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_logout"
        android:title="@string/global.menu.action.logout"
        app:showAsAction="never"/> <!-- esto hace que muestre en los 3 puntitos -->
</menu>